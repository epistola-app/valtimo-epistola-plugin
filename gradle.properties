# Gradle settings
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# Java 24 compatibility - enable native access
org.gradle.jvmargs=-XX:+EnableDynamicAgentLoading --enable-native-access=ALL-UNNAMED